# Example .env for backend
# Set your MongoDB connection string here (no deprecated query params needed)
MONGO_URI=mongodb://localhost:27017/moviesdb
PORT=4000
# Set to 1 to compute similarities at server startup
COMPUTE_ON_STARTUP=0
